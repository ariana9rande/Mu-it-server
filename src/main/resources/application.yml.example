server:
  port: # 8080

spring:
  application:
    name: # Mu:it

  profiles:
    # local, dev
    active: # local

  jpa:
    show-sql: # true

  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: your-app-password
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
    from: Mu:it

  data:
    redis:
      host: localhost
      port: 6379